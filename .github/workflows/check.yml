name: CI

on:
  push:
    branches: [ "main", "dev" ]
  pull_request:
    branches: [ "main", "dev" ]


jobs:
  tests:
    strategy:
      matrix:
        versions_python: ["3.10", "3.11", "3.12"]
        versions_lib: ["8.4.1", "8.4", "8.0"]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 
        uses: actions/checkout@v4

      - name: python download
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.versions }}

      - name: Instal deps
        run: pip install -r requirements.txt

      - name: Check
        run: python -m pytest